<% if (site.posts.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title"><i class="fa fa-archive"></i> <%= __('archive') %></h3>
    <div class="widget">
      <%- list_archives({show_count: theme.show_count, type: theme.archive_type, format: theme.archive_format}) %>
    </div>
  </div>
<% } %>
